<div>
  <h3 class="text-xl text-center font-semi-bold tracking-tight text-gray-900 py-4 truncate">
    <%= t('.title', product: @product.escape_value(:name)) %>
  </h3>

  <%= render 'variants/form', product: @product, variant: @variant,
              id: "new-product-#{@product.id}-variant" %>
</div>
