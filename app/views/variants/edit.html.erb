<div>
  <h3 class="text-xl font-semi-bold tracking-tight text-gray-900 py-4 truncate">
  <%= t('.title', product: @product.name) %>
  </h3>

  <%= render 'variants/form', product: @product, variant: @variant,
              id: "edit-product-#{@product.id}-variant-#{@variant.id}" %>
</div>
