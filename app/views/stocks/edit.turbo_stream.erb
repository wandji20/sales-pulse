<turbo-stream action='replace' target="<%= "edit-variant-#{@variant.id}-stock"%>">
  <template>
    <%= render 'stocks/form', stock: @stock, product: @product, variant: @variant %>
  </template>
</turbo-stream>