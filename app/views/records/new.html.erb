<div>
  <h3 class="text-xl text-center font-semi-bold tracking-tight text-gray-900 pb-4 truncate">
    <%= t('.title', name: @record.service? ? t('.service') : t('.sale')) %>
  </h3>

  <%= render 'records/form', record: @record, customers: @customers,
        variants: @variants, service_items: @service_items %>
</div>
