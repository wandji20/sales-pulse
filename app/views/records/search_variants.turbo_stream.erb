<%= turbo_stream.replace "variant-options" do %>
  <%= render "records/search/variants", variants: @variants,
        selected: params[:search_variant], list_class:  @variants.count > 0 ? '' : 'hidden' %>
<% end %>