<table class="table">
  <thead class='table-head'>
    <tr>
      <% User::HEADERS.each do |header| %>
        <th scope="col" class="<%= header_class(header) %>"><%= t(".#{header}") %></th>
      <% end %>
    </tr>
  </thead>
  <tbody class="table-body">
      <%= render partial: 'table_row', collection: customers, as: :customer %>
  </tbody>
</table>
